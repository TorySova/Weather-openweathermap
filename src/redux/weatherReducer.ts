const initState = {

};

export const weatherReducer = (state = initState, action: any): any => {
    switch (action.type) {
        case "": {
            return state;
        }
        default: return state;
    }
};

export const profileAC = (): any => {}; 